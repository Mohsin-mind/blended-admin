import DEFAULT_MSG from './defaultMsg';
import HTTP_ERROR_MSG from './httpErrorMsg';
import HTTP_STATUS from './httpStatus';
import MAGIC_NUMBERS from './magicNumbers';
import APP_META from './appMeta';
import FORM_VALIDATION from './formValidatoin';

const CONST = {
  HTTP_STATUS,
  HTTP_ERROR_MSG,
  MAGIC_NUMBERS,
  DEFAULT_MSG,
  APP_META,
  FORM_VALIDATION,
};

export default CONST;
